name: Build and Deploy to Azure

on:
  push:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install all dependencies
        run: npm ci

      - name: Build application
        run: npm run build
        env:
          VITE_STORE_HASH: ${{ secrets.VITE_STORE_HASH }}
          VITE_CHANNEL_ID: ${{ secrets.VITE_CHANNEL_ID || '1' }}
          VITE_LOCAL_APP_CLIENT_ID: ${{ secrets.VITE_LOCAL_APP_CLIENT_ID }}
          VITE_STORE_URL: ${{ secrets.VITE_STORE_URL }}
          VITE_IS_LOCAL_ENVIRONMENT: 'FALSE'

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v2
        with:
          app-name: feme-b2b-portal
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: .
